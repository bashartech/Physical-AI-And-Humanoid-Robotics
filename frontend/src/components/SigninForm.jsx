// import { authClient } from "../lib/auth-client";

// async function handleSignin(...) {
//   const { data, error } = await authClient.signIn.email({
//     email, password, name,
//     // optionally: callbackURL, image, etc.
//   });
//   if (error) {
//     alert(error.message);
//   } else {
//     // success — maybe redirect, or fetch session
//   }
// }
